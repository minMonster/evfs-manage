export default class User {
  constructor () {
    this.address = null
  }

  setAddress (add) {
    this.address = add
  }

  getAddress () {
    return this.address
  }

  checkLogin () {
    return this.address
  }
}
